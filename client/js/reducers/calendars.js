// import {
// } from '../constants/calendar';
//
export default function (state = [], action) {
  switch (action.type) {
    // case RELOAD_CALENDAR:
    //
    //   break;
    default:
      return state;
  }
}
